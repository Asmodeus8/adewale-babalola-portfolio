const catalog={Relay:['Durable webhook replay','Batched delivery','Delivery lag visibility'],Atlas:['Service ownership catalog','Runbook mapping','Dependency graph'],Pulse:['Regional synthetic probes','Percentile-aware alerts','Low-overhead monitoring'],Quarry:['PostgreSQL job leases','Retries and backoff','Dead-letter inspection'],Lattice:['Schema-first configuration','Pre-rollout validation','Edge-friendly SDK'],Northstar:['Environment comparison','Deployment diffing','Production change visibility'],Kiln:['Concurrent asset processing','Content fingerprinting','Deterministic manifests'],Beacon:['Incident chronology','Audit-log ingestion','Alert correlation'],Tandem:['Reverse proxying','Request mirroring','Circuit breaking'],Ledger:['Append-only accounting','Idempotent writes','Balance invariants'],Nomad:['Resumable streams','Signed jobs','Network tolerance'],Basalt:['Write-ahead log','B+ tree storage','Crash recovery'],Static:['Signed status updates','Incident subscriptions','SQLite persistence']};const dialog=document.querySelector('#project-dialog'),content=document.querySelector('#dialog-content');function wire(){document.querySelectorAll('.project-row').forEach((row,i)=>{const link=row.querySelector('.project-links a');if(!link)return;link.removeAttribute('href');link.removeAttribute('target');link.textContent='Details ↗';link.style.cursor='pointer';link.onclick=e=>{e.preventDefault();const name=row.querySelector('h3').textContent,lang=row.querySelector('.project-language').textContent,desc=row.querySelector('.project-description').textContent,tags=[...row.querySelectorAll('.tag')].map(x=>x.textContent),features=catalog[name]||tags;content.innerHTML=`<div class="detail-kicker">${lang}</div><h2>${name}</h2><p class="detail-lede">${desc}</p><div class="detail-rule"></div><section><h3>Why I built this</h3><p>An engineering build focused on understanding ${tags.slice(0,2).join(' and ')} beyond the framework layer, with clear failure modes and maintainable boundaries.</p></section><section><h3>Core features</h3><ul>${features.map(x=>`<li>${x}</li>`).join('')}</ul></section><section><h3>Technical approach</h3><p>The design keeps the core mechanics explicit and the operational surface small, making the system easier to reason about, test, and extend.</p></section><section><h3>Built with</h3><div class="detail-tags">${tags.map(x=>`<span>${x}</span>`).join('')}</div></section><section><h3>Source status</h3><p>The supplied portfolio package contains project metadata rather than the original source repository. A direct source link will be added when that codebase is available.</p></section><a class="detail-github" href="https://github.com/Asmodeus8" target="_blank" rel="noreferrer">View my GitHub →</a>`;dialog.showModal();document.body.classList.add('dialog-open')}})}new MutationObserver(wire).observe(document.querySelector('#project-list'),{childList:true});wire();document.querySelector('#dialog-close').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});dialog.addEventListener('close',()=>document.body.classList.remove('dialog-open'));